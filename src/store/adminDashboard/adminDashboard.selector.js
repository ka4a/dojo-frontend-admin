export const getDashboardCourses = ({ adminDashboard: { courses } }) => courses;

export const getDashboardRoles = ({ adminDashboard: { roles } }) => roles;

export const getDashboardNewInstructor = ({
  adminDashboard: { newInstructor },
}) => newInstructor;

export const getDashboardIsExpanded = ({ adminDashboard: { isExpanded } }) => isExpanded;
